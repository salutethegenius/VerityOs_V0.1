# Nova

Nova is the VerityOS AI operator. Production Nova remains the Content Loop deployment until a migrated runtime reaches feature parity. This monorepo does not modify that system in Phase 0–2.

Brands are plug-ins. Core logic must not hardcode brand names.

See [nova-behavior.md](../migration/nova-behavior.md). Runtime migration is Phase 8.
