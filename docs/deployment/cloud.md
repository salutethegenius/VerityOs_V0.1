# Cloud demonstration

Not configured in Phase 0–2. Future Railway/private-cloud profile uses the same application architecture: persistent Postgres, persistent `VERITY_DATA_DIR`, optional cloud or private models. Core VerityOS functionality must not require a cloud AI provider.

Production Nova remains on the existing Content Loop Railway service until parity is proven.
